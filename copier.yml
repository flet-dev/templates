_subdirectory: "{{ template_name.lower() }}"
_templates_suffix: ""
template_name:
  choices:
  - minimal
  - counter
  - flet_pack
  type: str
project_name: "{{ template_name }}"
description: "A {{ template_name }} Flet app"